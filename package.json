{
  "name": "ai-gov-form-assistant-root",
  "version": "1.0.0",
  "description": "Root runner to boot frontend and backend services concurrently",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run build --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
